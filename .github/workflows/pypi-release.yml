name: PyPI release

on:
  workflow_call:
    inputs:
      environment_name:
        description: GitHub environment name to use for trusted publishing.
        required: false
        type: string
        default: pypi
      pypi_project_name:
        description: PyPI project name used for the environment URL.
        required: false
        type: string
        default: ''

permissions: {}

jobs:
  release:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    environment:
      name: ${{ inputs.environment_name }}
      url: ${{ format('https://pypi.org/p/{0}', inputs.pypi_project_name || github.event.repository.name) }}
    permissions:
      contents: write
      id-token: write
    steps:
    - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      with:
        persist-credentials: false
    - uses: ./.github/actions/pypi-release

version: 2

multi-ecosystem-groups:
  default:
    schedule:
      interval: weekly

updates:
- package-ecosystem: github-actions
  directory: /
  patterns:
  - '*'
  groups:
    github-actions:
      patterns:
      - '*'
  cooldown:
    default-days: 7
  multi-ecosystem-group: default
